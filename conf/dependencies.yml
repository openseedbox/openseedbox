# Application dependencies

require:
    - play 1.2.5
    - play -> siena 2.0.7
    - openseedbox-common -> openseedbox-common
    
repositories:
    - modules:
       type: local 
       artifact: ${application.path}/../[module]
       contains:
          - local