# Application dependencies

require:
    - play 1.2.5
    - local -> openseedbox-common 0.1
    
repositories:
    - local:
       type: local 
       artifact: ${application.path}/../openseedbox-common/libs/[module]-[revision].jar
       contains:
          - local